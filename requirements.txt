# Core reconstruction dependencies (CoreSet-PFedBayes, ICLR 2024)
# Pinned loosely; tested with the versions in comments.
torch>=2.0            # 2.2.0
torchvision>=0.15     # 0.17.0
numpy>=1.24
scipy>=1.10
scikit-learn>=1.2
matplotlib>=3.7
h5py>=3.8
tqdm>=4.65
pyyaml>=6.0

# Phase 2 - medical datasets (Table 2)
medmnist>=3.0
submodlib-py>=0.0.2

# Phase 3 - Riemann linear-regression toy (Fig 2, Fig 3-KL) is vendored under
# src/experiments/riemann_linear_regression/bayesiancoresets/ and needs only
# numpy + matplotlib (bokeh/cairosvg replaced by a matplotlib backend).
